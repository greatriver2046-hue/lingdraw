/**
 * 外部配置文件
 * 此文件不会被Vite编译,部署后可直接修改
 * 更新项目时不需要覆盖此文件
 */
window.__APP_CONFIG__ = {
  // API基础地址
  API_BASE_URL: 'http://127.0.0.1:9998/api',
  
  // WebSocket地址(如果需要)
  WS_URL: 'wss://ws.xuku.cc/ws',
  
  // 其他需要外部配置的参数可以在这里添加
}
