可以，用宝塔面板按下面做即可（方案 A：走 443 反代到 2348）。

宝塔配置步骤

- 进入 宝塔面板 → 网站 → 你的域名（ws.xuku.cc 或 api.xuku.cc）→ 设置 → 配置文件
- 在对应的 server 块里添加一个 location /ws （注意要在 https 的 server 中）
- 保存并重载 Nginx
建议的 Nginx 配置片段

```
location /ws {
  proxy_pass http://127.0.0.1:2348;
  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection "upgrade";
  proxy_set_header Host $host;
  proxy_read_timeout 600s;
}
```
前端改成

```
VITE_WS_URL=wss://ws.xuku.cc/ws
```
注意点

- 该域名必须已在宝塔里启用 SSL 证书（面板的 SSL 里可一键申请 Let’s Encrypt）。
- 如果你希望和 API 用同一域名，也可以放在 api.xuku.cc ，那就配置 wss://api.xuku.cc/ws 。
如果你愿意，把你宝塔里该站点的 Nginx 配置贴出来，我可以直接帮你标注应该插入的位置。